{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Composer window: native folder picker, opening published URLs, and reading cached thumbnails.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:allow-open",
    "opener:allow-open-url"
  ]
}
