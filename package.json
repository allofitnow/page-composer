{
  "name": "page-composer",
  "version": "0.1.7",
  "private": true,
  "type": "module",
  "description": "AOIN project page composer — bridges asset folders + copy docs into a Payload-ready portfolio page.",
  "scripts": {
    "start": "node server/index.js",
    "tauri": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "node scripts/fetch-ffmpeg.mjs && tauri build",
    "fetch-ffmpeg": "node scripts/fetch-ffmpeg.mjs",
    "release": "node scripts/release.mjs"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mammoth": "^1.12.1",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0"
  }
}
